# STRICT ENVIRONMENT RULES
1. OS: Windows 11. Terminal: PowerShell.
2. YOU MUST NEVER use Linux/Bash commands (sed, awk, grep, rm, cat, touch, export) in the terminal.
3. FORBIDDEN: Do not use the terminal to edit, read, or replace text in files.
4. MANDATORY: You MUST use your native built-in tools (`replace_string_in_file`, `create_file`, `read_file`, `edit_notebook_file`) for ALL file manipulations.